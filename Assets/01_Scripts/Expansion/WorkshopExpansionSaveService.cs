using UnityEngine;

public class WorkshopExpansionSaveService : MonoBehaviour
{
    //private const string StageSaveKey = "WorkshopExpansion.CurrentStage";

    //public void SaveStage(int stage)
    //{
    //    int safeStage = Mathf.Max(0, stage);

    //    PlayerPrefs.SetInt(StageSaveKey, safeStage);
    //    PlayerPrefs.Save();
    //}

    //public int LoadStage()
    //{
    //    int savedData = PlayerPrefs.GetInt(StageSaveKey, defaultValue: 0);

    //    return Mathf.Max(0, savedData);
    //}
}
